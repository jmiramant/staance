.fundraise
  %ul
    %li.goal.stats
      Supporters
    %li.donation_total.stats
      = to_currency(@campaign.donation_total)
      of
      = to_currency(@campaign.funding_goal)
    %li.deadline.stats
      = @campaign.funding_deadline.strftime("Funding Deadline: %m/%d/%Y")
  #bar
    - if @campaign.donation_total < @campaign.funding_goal
      #progress_bg.small
        #progress{:style => "width: #{@campaign.donation_total.to_i/100}%;"}
    - else
      #progress_bg
        #progress
        #progress_text
          = @campaign.donation_total
  .donate
    .contribute
      = form_tag("/donate", method: "post", remote: true, class: "donate_payment") do
        = text_field_tag(:donation)
        = hidden_field_tag(:campaign_id, @campaign.id)
        = submit_tag("Donate!", class: 'button')



