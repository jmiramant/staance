- if user_signed_in?
  .support_button  
    = link_to "Support", campaigns_support_path(id: @campaign.id), method: 'post', id: "support_link", :class => 'unsupported', remote: true
-else
  .support_button
    = link_to "Support", "#", :class => 'unsupported', id: "support_link"
















